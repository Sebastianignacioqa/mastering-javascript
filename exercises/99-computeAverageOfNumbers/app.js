var computeAverageOfNumbers = (array) => {
    let total = 0
  
  if (obj[key].length == 0){
    return 0;
  }
    for (var i in obj.key) {
    total += obj.key[i];
  }
  return (total/obj.key.length);
  
 }
// AL PARECER EL TEST O EL EJERCICIO ESTAN MAL, YA QUE LA SOLUCION PIDE UNA COSA DISTINTA QUE EL ENUNCIADO.